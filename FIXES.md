# Fix Log

| File | Line | Problem | Change Made | Reason |
|---|---:|---|---|---|
| `frontend/app.js` | 6 | API URL used `localhost`, which points to the frontend container itself in Docker networking. | Replaced with `process.env.API_URL || 'http://api:8000'`. | Makes API endpoint environment-driven and container-safe. |
| `frontend/app.js` | 16, 28 | Upstream API failures were returned as generic 500s, hiding dependency failure semantics. | Changed fallback responses to `502` and added explicit `404` passthrough for missing jobs. | Improves correctness and observability between frontend and API errors. |
| `frontend/views/index.html` | 23-35 | `submitJob()` had no error handling and assumed every response contains `job_id`. | Wrapped call in `try/catch` and validated `res.ok` + `data.job_id` before polling. | Prevents UI breakage on transient/API errors. |
| `frontend/views/index.html` | 38-54 | `pollJob()` could loop forever when API response had no `status` or failed. | Added `try/catch` and explicit checks for `res.ok` + `data.status`; render `failed` state when invalid. | Prevents infinite polling and gives deterministic failure state. |
| `api/main.py` | 8-11 | Redis host/port had no defaults and `REDIS_PORT` was not cast to int. | Added env defaults (`redis`, `6379`) and cast port with `int(...)`. | Ensures stable startup across local and container environments. |
| `api/main.py` | 23-24 | Missing job returned JSON with HTTP 200 instead of a 404. | Imported `HTTPException` and now raise `HTTPException(status_code=404, detail='job not found')`. | Restores proper API semantics for not-found resources. |
| `worker/worker.py` | 5-8 | Redis connection relied on env values without defaults and port type conversion. | Added env defaults (`redis`, `6379`) and `int(...)` for port. | Prevents runtime misconfiguration failures in containerized environments. |
| `worker/worker.py` | 4 | Unused `signal` import. | Removed unused import. | Avoids lint failures and keeps runtime code clean. |
| `api/.env` | 1-3 | Committed file contained hardcoded secret value and `localhost` host unsuitable for container networking. | Removed secret entry and switched `REDIS_HOST` to `redis`. | Eliminates secret leakage and aligns runtime config with Docker networking. |
| `worker/.env` | 1 | `REDIS_HOST` was `localhost`, breaking worker-to-Redis communication in containers. | Set `REDIS_HOST=redis`. | Uses Compose service DNS name instead of loopback. |
| `frontend/.env` | 1 | API URL pointed to `localhost`, which fails from inside the frontend container. | Set `API_URL=http://api:8000`. | Aligns frontend runtime config with internal service discovery. |
| `api/main.py` | 13-15 | API had no lightweight health endpoint for container probes. | Added `GET /health` route returning `{"status":"ok"}`. | Enables Docker `HEALTHCHECK` and Compose health-based dependencies. |
| `docker-compose.yml` | — | Compose file was invalid (missing `services:` root, bad indentation, and no Redis service wiring). | Rewrote into valid multi-service Compose with `frontend`, `api`, `worker`, `redis`, named network, health-based `depends_on`, and resource limits. | Makes the stack runnable and satisfies production orchestration requirements. |
| `frontend/.env.example` | 1 | Environment key typo (`Api_URL`) would not be read by Compose/app expecting `API_URL`. | Corrected to `API_URL`. | Prevents silent misconfiguration during setup. |
| `.env.example` | — | Missing single root env template meant no unified variable source for full-stack Compose startup. | Added root `.env.example` with placeholders (including `IMAGE_TAG`), defaults for limits. | Meets documentation/config requirement and simplifies clean-machine setup. |
| `api/main.py` | 18-27 | PEP8 E302 (`flake8`): fewer than two blank lines between route handlers after edits. | Inserted blank lines between `@app.post` / `@app.get` blocks. | Satisfies Python style checks used in CI. |
| `frontend/app.js` | 15 | ESLint `no-unused-vars`: catch binding unused in `/submit`. | Switched to optional catch binding (`catch { ... }`). | Keeps ESLint passing without losing error handling. |
| — | — | Missing automated tests for API (assignment requires pytest + mocked Redis). | Added `api/tests/test_main.py` (≥3 cases), `api/requirements-dev.txt`, `api/pytest.ini`; Redis mocked via `unittest.mock`. | Enables deterministic CI tests without a real Redis. |
| — | — | Missing frontend lint toolchain (assignment requires ESLint). | Added `frontend/eslint.config.cjs`, ESLint devDependencies, `npm run lint`. | Provides JS lint gate in CI. |
| `.hadolint.yaml` | — | Starter `requirements.txt` uses unpinned versions; Hadolint DL3013 would fail builds. | Added `.hadolint.yaml` ignoring DL3013 (documented tradeoff). | Keeps Dockerfile lint usable without forcing full pinning pass in this starter task. |
| `.flake8` | — | No shared flake8 config for consistent line length / exclusions. | Added root `.flake8` for API and CI. | Aligns local and CI Python lint behavior. |
| `.gitignore` | — | Risk of committing `.env`, bytecode, `node_modules`, and coverage artifacts. | Added ignore rules; allow `**/.env.example` via negation patterns. | Keeps secrets and generated files out of git per assignment rules. |
| `docker-compose.ci.yml` | — | CI/CD needs Compose that references pre-built images (`jobproc-*:${IMAGE_TAG}`) instead of `build:`. | Added compose file consumed by integration/deploy scripts and GitHub Actions jobs. | Separates local-dev `build:` flow from artifact-driven CI runs. |
| `scripts/integration_test.sh` | — | Requirement: integration test submits via frontend and polls completion with clean teardown. | Scripted stack up (`--wait`), POST `/submit`, poll `/status/:id`, `trap` cleanup `down -v`. | Validates end-to-end behavior on the runner. |
| `scripts/deploy_rolling.sh` | — | Requirement: rolling API update with 60s health gate; abort leaves old API running. | Start redis/worker/api; run candidate `api_roll_candidate` on `app_net`; loop health exec; swap only on success. | Matches rolling semantics for the API service on `main`. |
| `.github/workflows/ci.yml` | — | Requirement: strict pipeline order lint→test→build→scan→integration→deploy. | Added workflow with job `needs` chain, local registry push in `build`, Trivy SARIF artifacts, integration + conditional deploy. | Automates grading-style delivery on GitHub free tier. |
